{{#power-select
  options=options
  selected=value
  disabled=disabled
  triggerId=controlId
  onchange=on-change
  searchEnabled=false
  triggerClass="oe-control__select-trigger"
  dropdownClass="oe-control__select-dropdown"
  as |item|}}
  {{item}}
{{/power-select}}
