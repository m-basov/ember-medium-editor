{{#me-extension
  disabled=disabled
  registerExtension=registerExtension
  as |ext|}}
  {{yield (hash
    registerCommand=(action ext.pushChild "commands")
    command=(component "keyboard-command" registerCommand=(action ext.pushChild "commands"))
  )}}
{{/me-extension}}
